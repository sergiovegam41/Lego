# CONFIGURACION PARA SERVICIOS EN LA NUBE
# Copia este archivo a .env y completa con tus credenciales

# Url publica
HOST_NAME=http://localhost:8080

# Configuración de PostgreSQL - Neon.tech (Gratuito)
# Registrate en https://neon.tech y crea una base de datos
# Formato: postgresql://username:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
DB_HOST=ep-your-endpoint.region.aws.neon.tech
DB_PORT=5432
DB_DATABASE=your-database-name
DB_USERNAME=your-username
DB_PASSWORD=your-password

# Configuración de MongoDB - MongoDB Atlas (Gratuito M0)
# Registrate en https://mongodb.com/atlas y crea un cluster M0
# Formato: mongodb+srv://username:password@cluster.mongodb.net/database
MONGO_DB_HOST=your-cluster.mongodb.net
MONGO_DB_PORT=27017
MONGO_DB_DATABASE=your-database
MONGO_DB_USERNAME=your-username
MONGO_DB_PASSWORD=your-password

# Configuración de Redis - Upstash (Gratuito)
# Registrate en https://upstash.com y crea una base de datos Redis
REDIS_HOST=your-redis-host.upstash.io
REDIS_PORT=6379
REDIS_USER=default
REDIS_PASSWORD=your-redis-password

# Configuración de Jwt
JWT_SECRET=your-secure-jwt-secret-here

# Identificadores de usuario
UID=1000
GID=1000

# Configuración de n8n - Deshabilitado para simplificar
N8N_ENABLED=false
N8N_LOCAL_INSTANCE=false

# Configuración de n8n Local
N8N_PORT=5678
N8N_PROTOCOL=http
DOMAIN_NAME=localhost
N8N_ENCRYPTION_KEY=your-secret-key-min-32-characters-termux
NODE_ENV=development

# Base de datos para n8n (usar la misma de PostgreSQL si se habilita)
N8N_DB_TYPE=postgresdb
N8N_DB_HOST=ep-your-endpoint.region.aws.neon.tech
N8N_DB_DATABASE=n8n
N8N_DB_USER=your-username
N8N_DB_PASSWORD=your-password
N8N_DB_PORT=5432
GENERIC_TIMEZONE=UTC
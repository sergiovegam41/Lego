<?php
/**
 * CONFIGURACIÓN DE PRUEBAS PARA DESARROLLO
 *
 * Para probar el sistema de extensión de tokens rápidamente,
 * usa estos valores en AuthServicesCore.php:
 */

// OPCIÓN A: Pruebas muy rápidas (tokens de 5 segundos)
private $jwt_expire = 5; // 5 segundos
private $token_extension_threshold = 3; // Extender cuando queden 3 segundos (60% del tiempo)
private $activity_check_interval = 2; // Actualizar BD cada 2 segundos

// NOTA: Con esta configuración:
// - El token expira en 5 segundos
// - Se extiende cuando quedan 3 segundos o menos
// - Verás logs muy frecuentemente
// - La BD se actualiza cada 2 segundos

// OPCIÓN B: Pruebas moderadas (tokens de 1 minuto)
private $jwt_expire = 60; // 1 minuto
private $token_extension_threshold = 30; // Extender cuando queden 30 segundos (50% del tiempo)
private $activity_check_interval = 10; // Actualizar BD cada 10 segundos

// OPCIÓN C: Producción (tokens de 1 hora) - RECOMENDADO
private $jwt_expire = 3600; // 1 hora
private $token_extension_threshold = 1800; // Extender cuando queden 30 minutos (50% del tiempo)
private $activity_check_interval = 300; // Actualizar BD cada 5 minutos

/**
 * IMPORTANTE: La regla es que threshold debe ser MENOR que jwt_expire
 *
 * Típicamente: threshold = 50-70% de jwt_expire
 *
 * Ejemplos válidos:
 * - jwt_expire: 5 seg  → threshold: 3 seg  (60%)
 * - jwt_expire: 60 seg → threshold: 30 seg (50%)
 * - jwt_expire: 3600 seg → threshold: 1800 seg (50%)
 *
 * Ejemplo INVÁLIDO:
 * - jwt_expire: 5 seg → threshold: 10 seg ❌ (nunca se extenderá!)
 */
